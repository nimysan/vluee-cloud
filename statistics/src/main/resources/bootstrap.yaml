server:
  port: 8903
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

spring:
  application:
    name: saas-statistics
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848