server:
  port: 8902
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

spring:
  application:
    name: saas-search
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848