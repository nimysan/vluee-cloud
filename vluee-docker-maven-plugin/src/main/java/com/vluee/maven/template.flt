version: "3"
services:
  ${serviceName}:
    image: '${image}'
    <#if port??>
    ports:
      - "${port}:8080"
    </#if>
    deploy:
      replicas: 1
    networks:
      - ${networkName}

networks:
  ${networkName}:
    external: true